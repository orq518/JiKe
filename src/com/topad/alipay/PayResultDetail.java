package com.topad.alipay;import android.text.TextUtils;import java.util.HashMap;import java.util.Map;public class PayResultDetail {	public String out_trade_no;	public String seller_id;	public String subject;	public String body;	public String total_fee;	public PayResultDetail(String rawResult) {		if (TextUtils.isEmpty(rawResult))			return;		String[] resultParams = rawResult.split("&");		for (String resultParam : resultParams){			String[] arrSplitEqual = null;			arrSplitEqual = resultParam.split("[=]");			if (arrSplitEqual[0].equals("out_trade_no")) {				out_trade_no = arrSplitEqual[1];			}else if (arrSplitEqual[0].equals("seller_id")) {				seller_id = arrSplitEqual[1];			}else if (arrSplitEqual[0].equals("subject")) {				subject = arrSplitEqual[1];			}else if (arrSplitEqual[0].equals("body")) {				body = arrSplitEqual[1];			}else if (arrSplitEqual[0].equals("total_fee")) {				total_fee = arrSplitEqual[1];			}		}	}	public String getout_trade_no() {		return out_trade_no;	}	public String getseller_id() {		return seller_id;	}	public String getsubject() {		return subject;	}	public String getbody() {		return body;	}	public String gettotal_fee() {		return total_fee;	}	/**	 * 解析出url参数中的键值对	 * 如 "index.jsp?Action=del&id=123"，解析出Action:del,id:123存入map中	 *	 * @param URL url地址	 * @return url请求参数部分	 *//*	public static Map<String, String> URLRequest(String URL) {		Map<String, String> mapRequest = new HashMap<String, String>();		String[] arrSplit = null;		String strUrlParam = URL;		if (strUrlParam == null) {			return mapRequest;		}//每个键值为一组		arrSplit = strUrlParam.split("[&]");		for (String strSplit : arrSplit) {			String[] arrSplitEqual = null;			arrSplitEqual = strSplit.split("[=]");//解析出键值			if (arrSplitEqual.length > 1) {//正确解析				mapRequest.put(arrSplitEqual[0], arrSplitEqual[1]);			} else {				if (arrSplitEqual[0] != "") {//只有参数没有值，不加入					mapRequest.put(arrSplitEqual[0], "");				}			}		}		return mapRequest;	}*/}